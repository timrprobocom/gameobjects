# gameobjects

This repo is a simple port to Python3 of the `gameobjects` module that
Will McGugan wrote for his book.  I claim no ownership; I did a simple
port.  The original page is  https://pypi.org/project/gameobjects/ .

Run the following to install:

python setup.py install 

